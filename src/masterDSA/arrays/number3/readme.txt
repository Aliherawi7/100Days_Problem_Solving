

### 3. Find The Contiguous Subarray

descriptions:
Given an integer array, find the contiguous
subarray (containing a least one number)
which has the largest sum and return its
sum and print the subarray.

example 1:

input = [6,9,2,3,4,5,7]